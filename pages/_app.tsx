import '../styles/sass/tailwind.style.scss';
import '../styles/sass/animation/cloud.style.scss';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
